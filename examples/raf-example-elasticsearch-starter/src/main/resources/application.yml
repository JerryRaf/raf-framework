spring:
  application:
    name: raf-example-elasticsearch-starter
  elasticsearch:
    uris: ${ES_URI:http://localhost:9200}
    username: ${ES_USER:}
    password: ${ES_PASS:}
server:
  port: 8080
raf:
  log:
    enabled: true
    level: REQ_BODY
