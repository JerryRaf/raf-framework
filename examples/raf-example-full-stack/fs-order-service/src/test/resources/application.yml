spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:

seata:
  enabled: false

dubbo:
  registry:
    address: N/A
  consumer:
    check: false

rocketmq:
  name-server: localhost:9876
